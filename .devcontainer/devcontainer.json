{
	"name": "Python 3 RAG App",
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"forwardPorts": [5000],
	"postCreateCommand": "pip install -r requirements.txt && ollama pull qwen2:1.5b",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}
