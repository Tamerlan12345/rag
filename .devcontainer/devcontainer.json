{
	"name": "Python 3 RAG App",
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"forwardPorts": [5000],
	"postCreateCommand": "pip install -r requirements.txt && ollama serve & ollama pull mistral:7b-instruct-q4_K_M",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}
