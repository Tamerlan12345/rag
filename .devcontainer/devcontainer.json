{
	"name": "Python 3 RAG App",
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"forwardPorts": [5000],
	"postCreateCommand": "pip install -r requirements.txt && ollama pull gemma:2b",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}
