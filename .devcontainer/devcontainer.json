// .devcontainer/devcontainer.json
{
	"name": "Python 3 RAG App",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"forwardPorts": [5000],

	"postCreateCommand": "pip install -r requirements.txt && ollama pull llama3",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python"
			]
		}
	}
}
